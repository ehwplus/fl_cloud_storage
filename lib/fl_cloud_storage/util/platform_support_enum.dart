/// Right now Flutter supports 6 operation systems. In many cases the
/// cloud storage provider is not available on all platforms.
enum PlatformSupportEnum {
  ANDROID,
  IOS,
  WEB,
  LINUX,
  MACOS,
  WINDOWS;
}
